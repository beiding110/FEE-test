function FrR (obj) {
  this.init(obj)
}

FrR.prototype = {
  init (obj) {
    this.$setting = obj
  },
  build (path) {
    var input = path
    var output = input
    return output
  }
}

export default FrR
